// Shared grid constants — used by both SnakeGame and page layout
export const CELL       = 45;   // px per grid cell
export const FORM_COLS  = 15;   // form card width in cells  (15 × 45 = 675 px)
export const FORM_ROWS  = 8;    // snake exclusion zone height (cells, includes padding)
