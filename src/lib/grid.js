// Shared grid constants — used by both SnakeGame and page layout
export const CELL       = 20;   // px per grid cell
export const FORM_COLS  = 28;   // form card width in cells  (28 × 20 = 560 px)
export const FORM_ROWS  = 20;   // snake exclusion zone height (cells, includes padding)
