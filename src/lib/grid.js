// Shared grid constants — used by both SnakeGame and page layout
export const CELL       = 15;   // px per grid cell
export const FORM_COLS  = 46;   // form card width in cells  (46 × 15 = 690 px)
export const FORM_ROWS  = 24;   // snake exclusion zone height (cells, includes padding)
